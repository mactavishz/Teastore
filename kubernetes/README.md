# Kubernetes Infomation

All resources are in the namespace `teastore`.

# How to run with kustomize

```bash
# start
kubectl apply -k ./

# update
kubectl apply -k ./

# stop
kubectl delete -k ./
```